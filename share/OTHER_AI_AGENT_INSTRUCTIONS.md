# Other AI Agent — AI Memory OS V4
If you can read and run local files on the current machine, read `$HOME/AI-Memory/AI_MEMORY_AGENT_PROTOCOL_V4.md` and follow it for
registered projects: `aimem-detect` -> `aimem begin` (capture SESSION_ID, read CONTEXT) -> `aimem-step --session <id>` for
meaningful steps -> update CURRENT.md/NEXT.md -> `aimem finish --session <id>` with a truthful result. Use `--agent other`.
Local only. No secrets. Fresh verified physical state overrides stale memory.
